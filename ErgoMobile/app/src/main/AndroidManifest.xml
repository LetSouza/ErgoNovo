<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ergo.leticia.ergomobile">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.RECORD_VIDEO" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity_"
            android:label="@string/ErgoMobile"/>
        <activity
            android:name=".activity.MainTela2"
            android:label="@string/ErgoMobile"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.TesteAudioActivity"
            android:label="@string/TesteAudio" />
        <activity
            android:name=".activity.TesteVideoActivity"
            android:label="@string/TesteVideo" />
        <activity
            android:name=".activity.TesteUsabilidadeActivity"
            android:label="@string/TesteUsabilidade" />
        <activity
            android:name=".activity.FinalActivity"
            android:label="@string/FinalTeste" />

        <service
            android:name=".service.BackgroundService"
            android:enabled="true" />
        <service
            android:name=".service.RecorderService"
            android:enabled="true" />
        <service
            android:name=".service.BackgroundAudioService"
            android:enabled="true" />
        <service
            android:name=".service.AudioService"
            android:enabled="true" />
        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".activity.SplashActivity_"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Black.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>